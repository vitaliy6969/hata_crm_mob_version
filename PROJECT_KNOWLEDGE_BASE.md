# 📔 Довідник HataCRM (Knowledge Base)

Цей документ об'єднує бізнес-логіку `RentalBusiness` та стратегію **«Цифровий Адміністратор»**.

---

## 🎯 Концепція «Цифровий Адміністратор»
Система має замінити людину-адміністратора, об'єднуючи:
*   **Входи**: Дзвінки (SIP), Месенджери (Viber/TG/WA), n8n, Сайт.
*   **Клієнтський простір (`/client`)**: WiFi, ключі, чат, підпис договору (PDF з печаткою).
*   **Адмінка (`/admin`)**: Черга «Потрібне уточнення», скринінг (чорний список), договори, бухгалтерія.

---

## 🏠 Об'єкти (Квартири) - Лютий 2026
16 активних об'єктів (див. деталі у проекті). 
*   **Спец-випадок**: Бориса Тена 1/2 — комунальні навпіл.

---

## ⚙️ Бізнес-Правила

### 1. Правило «Першої ночі»
Дохід за ніч завжди йде в день **ЗАЇЗДУ**.

### 2. Скринінг та Відповідальність
*   **Чорний список**: Автоматична перевірка за номером/ПІБ при кожному запиті.
*   **Цифровий договір**: Бронь вважається остаточно підтвердженою тільки ПІСЛЯ графічного підпису гостем договору на сайті та надання згоди на обробку даних.

---

## 🤖 Технічний Roadmap (Оновлено)

### Етап 1 (Зараз): База та Шахматка
*   [x] PostgreSQL та список 16 квартир.
*   [x] Динамічна Шахматка.
*   [ ] **Авто-розрахунок ціни** (на основі бази).

### Етап 2: Робота з Бронюванням та Клієнтами
*   [ ] Черга «На уточнення» (адмін-дашборд).
*   [ ] Картка клієнта + Історія + Чорний список.
*   [ ] Система цифрових договорів (Web-підпис).

### Етап 3: Персонал та Фінанси
*   [ ] Телеграм-бот для покоївок.
*   [ ] Автоматичні дашборди Income/Profit (як були в Python-скриптах).

### Етап 4: Комунікації
*   [ ] Інтеграція месенджерів та телефонії (SIP).

---

## 🗄 Локально пусто — база на сервері

Якщо запускаєш додаток **локально** (`npm start`), а в `.env` стоїть `DATABASE_URL=...@localhost:5433/...`, то квартири й броні беруться з **локальної** бази. Вона часто порожня — тому й тут пусто.

**Щоб локально бачити ті самі дані, що на сайті:**

1. Відкрий `.env`.
2. Замість локального рядка підключення вкажи **серверну** базу (хост, порт, користувач, пароль, ім'я БД — як на сервері).  
   Формат: `postgres://USER:PASSWORD@HOST:PORT/DATABASE`  
   Приклад: `postgres://hata_user:secret@my-server.com:5432/hata_db`
3. Перезапусти додаток (`npm start`).

**Важливо:** серверна PostgreSQL має дозволяти підключення з твоєї мережі (налаштування `pg_hba.conf` та файрволу). Якщо доступу з інтернету немає, можна підняти **SSH-тунель** до сервера і підключатися через `localhost` у тунелі.
